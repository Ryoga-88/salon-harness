import { AppShell } from '@/components/app-shell';

export default function MessagesPage() {
  return (
    <AppShell>
      <h1 className="page-title">メッセージ</h1>
      <section className="panel">
        <p className="muted">line-harness のシナリオ、配信、リッチメニュー API をここへ統合します。</p>
      </section>
    </AppShell>
  );
}
