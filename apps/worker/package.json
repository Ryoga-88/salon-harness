{
  "name": "@salon-harness/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "@salon-harness/salon-domain": "workspace:*",
    "@salon-harness/shared": "workspace:*",
    "bcryptjs": "^3.0.3",
    "hono": "^4.10.7",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.1",
    "@cloudflare/workers-types": "^4.20260502.0",
    "vitest": "^3.2.4"
  }
}
